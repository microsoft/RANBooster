To build, you need to first install Intel OneApi for the icx compiler.

Assuming that OneApi is installed under /opt/intel/oneapi, you need to source the env variables:

```bash
source /opt/intel/oneapi/setvars.sh
```

and then run `make`.
